-- 
-- 
-- Create a new Database and a new table inside that database with these credentials, or you may tinker around a bit and adjust it to your needs
-- 
-- 
CREATE DATABASE todo_database;

CREATE TABLE todo_table(id BIGINT PRIMARY KEY, task VARCHAR(16387), done BOOLEAN);
